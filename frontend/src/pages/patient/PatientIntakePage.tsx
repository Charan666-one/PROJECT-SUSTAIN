/**
 * PatientIntakePage — mobile-first form filled by patient BEFORE consultation
 * Accessible via unique WhatsApp link sent by clinic
 * No login required — token-based access
 * Multilingual support
 */
export default function PatientIntakePage() {
  // Steps: 1. Basic info  2. Chief complaint  3. Symptom details
  //        4. Concurrent medications  5. Lifestyle  6. Consent
  return (
    <div className="intake-form-container">
      {/* Multi-step form — mobile optimised */}
    </div>
  );
}
